# Application
NODE_ENV=development
PORT=3000

# Database - Option 1: Use separate variables (for dev and docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=gamedev_catalogue

# Database - Option 2: Use connection URL (production default)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gamedev_catalogue

# pgAdmin (for docker-compose)
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin

# Note: This project uses the 'config' npm package with TypeScript files.
# Configuration is managed through config/*.ts files.
# Environment variables override the config files via config/custom-environment-variables.ts
# See config/README.md for detailed configuration documentation.
